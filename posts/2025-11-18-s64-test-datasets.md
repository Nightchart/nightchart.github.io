---
title: S-64：IHO 官方测试数据集，渲染器的陪练
slug: s64-test-datasets
date: 2025-11-18
description: 入门路线里反复提到的 S-64 到底是什么：IHO 官方的 ECDIS 测试数据集。三类人三种用法、一条更新用例的五步走法、三大翻车点对照表，以及怎么把"人眼比对"升级成像素 diff 回归。
tags: S-64, 测试数据集, ECDIS, 渲染回归, IHO
draft: true
---

第一篇入门文里给过一条最便宜的上手路线：OpenCPN + IHO S-64 官方测试数据 + GDAL，一周建立"数据→显示"的直觉。前半句和后半句都好理解，中间这个 **S-64** 一直没展开。一句话定位：**它是 IHO 出的 ECDIS 测试数据集，专门回答"你的渲染器显示得对不对"**。

标准文本（S-52、S-57）写的是"应该怎样"；S-64 给的是**现成的数据加标准答案**——一份精心构造的 ENC 数据，配套一批"加载后你应该看到什么"的用例说明与预期显示效果。自研渲染器最痛苦的一步是手里没有"对的参照物"，S-64 就是那个参照物。

## 三类人，三种用法

- **ECDIS 合规视角**：显示与更新是否符合 S-52/S-57 的表现要求，是型式认可审查的实质内容之一。S-64 长期扮演验收比对基准的角色——按用例逐条过，差异即疑点
- **开源/自研渲染器视角**：OpenCPN 这类实现和各家自研引擎，拿它当**对照系**——同一份数据，你画的、别人画的、预期效果，三者对齐说明理解没跑偏
- **入门者视角**：初学最难的不是代码，是"不知道自己画得对不对"。S-64 每条用例附带现象说明，等于自带答案册

## 一条更新用例的完整走法

拿最能暴露问题的"更新链路"用例举例（各版本 S-64 的用例组织不同，以随包说明为准），走法固定五步：

![五步工作流 + 像素 diff 循环](assets/fig-s64-flow.svg)

1. **装底座**：OpenCPN 正常安装，把 S-64 数据集所在目录加入海图集合，加载基础数据，截图记下初始画面
2. **逐份应用更新**：数据集内的增量更新文件逐份导入。要盯的不是一个静态结果，而是**每一次应用后的画面变化**：浮标移位、沉船新增、等深线改值导致周边填色重排——每一处变化都对应标准里的一条表现要求
3. **对照预期**：用例说明写明了预期现象，逐条打钩
4. **数据侧核对**：有疑问时用 GDAL 把同一份数据的要素、属性、几何 dump 出来，先分清"数据理解错了"还是"画错了"——这两类错的排查方向完全相反
5. **记错题本**：把翻车用例记下来。它们就是你私有的 S-64，未来回归测试的种子

前三步手点一遍大约半天，价值在于理解每条用例在防什么错。**手点第二遍开始就是在浪费生命**——所以有第四步之后的自动化。

## 三大翻车点对照表

大量排查时间其实花在同一类问题上。按出现频率排个序：

| 现象 | 根因 | 第一排查动作 |
|---|---|---|
| 更新应用了，画面没变 | 某层缓存的失效没接到这次更新 | 关缓存直渲染复现，先定性再定位 |
| 物标画了，符号不对 | look-up 表查错：条件项、状态档位或专用符号匹配 | 打印该要素命中的 look-up 条目，逐项核对 |
| 颜色"差不多但不全对" | token 用错色板，或硬编码了十六进制值 | 从渲染代码里捞出该像素的 token 名，回色板表对照 |

查错顺序也值得写死：**先查 look-up 和配置，再查绘制代码**——前者错得最多，后者背锅最多。

## 从人眼到像素 diff

把"预期效果 vs 实际渲染"自动化，只需要**三固定**：固定输入、固定渲染、固定比对。

- **固定输入**：同一份数据文件、同一套 look-up 配置、同一个视口；涉及时间的要素（灯标光韵律这类）固定时钟
- **固定渲染**：渲染一帧，截图保存为基准图（baseline）；此后每次代码变动，重画同一帧
- **固定比对**：逐像素做通道差分，容差以内（抗锯齿边缘的灰度抖动）忽略；超差的连通区域聚类、框出、高亮——一份差异报告就是本次改动的影响清单

比对函数本身只有几十行，核心逻辑一看就懂：

```js
// diff.js：基准图 vs 当前帧（RGBA，逐像素比较）
function diff(baseline, current, tol = 8) {
  const bad = [];
  for (let i = 0; i < baseline.length; i += 4) {
    if (Math.abs(baseline[i]   - current[i])   > tol ||
        Math.abs(baseline[i+1] - current[i+1]) > tol ||
        Math.abs(baseline[i+2] - current[i+2]) > tol) {
      bad.push(i / 4);                   // 记下超差像素的坐标
    }
  }
  return cluster(bad, baseline.width);   // 相邻超差点聚成矩形框
}
```

两个工程细节决定这玩意儿是"真好用"还是"天天狼来了"：

- **可容忍区域做成掩膜**：状态栏时钟、天然渐变的水面纹理这类必然变化的区域，从比对中剔除——否则每天第一次运行都是假警报
- **固定输入要真的固定**：涉及时间的要素（灯标光韵律、潮汐时刻）给渲染器喂固定 UTC，随机数喂固定种子

这条 diff 线的成本是一个截图脚本加一个比对函数，收益是**每一次升级依赖、移植算法、调整 look-up 都有保险**。配套还要立一条纪律：diff 报红只有两种合法处理——修 bug，或者确认是"有意变更"并评审后更新基准图。没有第三种，基准图悄悄换掉的那天，回归线就名存实亡。

我们的回归线跑起来后抓到的第一个问题，是一个只在特定符号密度下出现的注记错位，手测根本撞不见。这种案子攒多了你会发现：**S-64 的价值不在"通过"，而在把"显示得对不对"从一道主观题变成一道判断题。**

## 两个提醒

S-64 覆盖的是**典型与疑难场景的抽样**，不是全量验收——通过它不代表合规达标，不通过一定有问题。另外它随版本演进、用例集持续增补（S-101 时代的测试数据也在路上），跟着当前版本的随包说明走，别背旧版目录。

下一篇回到数据侧：S-125 航标（AtoN）——灯标"灭没"这种短周期变更，标准里是怎么建模的。

---

我是夜航海图，做海图与地图渲染开发的工程师。博客「航图笔记」同步更新全部文章，欢迎 RSS 订阅。
